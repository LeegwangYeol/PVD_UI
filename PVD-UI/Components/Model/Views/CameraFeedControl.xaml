<UserControl x:Class="PVD_UI.Components.Model.Views.CameraFeedControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="900">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Button.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid Margin="-4,0,4,0">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Background="#27272a" CornerRadius="8" Margin="52,20,8,86" BorderBrush="#00FF00" BorderThickness="2">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="Camera" Foreground="Gray" Width="32" Height="32"/>
                <TextBlock Text="Camera 1" Foreground="Gray" FontSize="12" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="0" Grid.Column="2" Background="#27272a" CornerRadius="8" Margin="51,20,2,80" BorderBrush="#00FF00" BorderThickness="2">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="Camera" Foreground="Gray" Width="32" Height="32"/>
                <TextBlock Text="Camera 2" Foreground="Gray" FontSize="12" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="2" Grid.Column="0" Background="#27272a" CornerRadius="8" Margin="69,86,27,20" BorderBrush="#00FF00" BorderThickness="2">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="Camera" Foreground="Gray" Width="32" Height="32"/>
                <TextBlock Text="Camera 3" Foreground="Gray" FontSize="12" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="2" Grid.Column="2" Background="#27272a" CornerRadius="8" Margin="44,76,4,20" BorderBrush="#00FF00" BorderThickness="2">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="Camera" Foreground="Gray" Width="32" Height="32"/>
                <TextBlock Text="Camera 4" Foreground="Gray" FontSize="12" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
